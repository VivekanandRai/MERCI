/** @type {import('next').NextConfig} */
const nextConfig = {
  // For GitHub Pages: uncomment the next 3 lines
  // output: 'export',
  // basePath: '/MERCI',
  // images: { unoptimized: true },
};

export default nextConfig;
